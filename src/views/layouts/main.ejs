<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADN Crea</title>

</head>
<body>

    <% if (CorreoExistente.length > 0) { %>
        <h2><%= CorreoExistente %></h2>
    <% } %>
      
    <% if (CorreoNoEncontrado.length > 0) { %>
        <h2><%= CorreoNoEncontrado %></h2>
    <% } %>
    
    
    <% if (Contraseña.length > 0) { %>
        <h2><%= Contraseña %></h2>
    <% } %>

    <%- body %>


</body>
</html>